"""Single source of truth for the FitForge release version.

Both the Python package and the deployment tooling read this constant
so a deploy never disagrees with the running code.
"""
__version__ = "1.0.0"
